import styled from "styled-components";

// this Wrapper is the select
export const Wrapper = styled.select`
  width: 100px;
  border-radius: 50px;
  background-color: white;
`;

export const Option = styled.option``;

export const Span = styled.span``;
